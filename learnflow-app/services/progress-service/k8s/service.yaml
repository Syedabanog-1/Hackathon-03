apiVersion: v1
kind: Service
metadata:
  name: progress-service
  namespace: learnflow
  labels:
    app: progress-service
spec:
  selector:
    app: progress-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8005
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: triage-service
  namespace: learnflow
  labels:
    app: triage-service
spec:
  selector:
    app: triage-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP

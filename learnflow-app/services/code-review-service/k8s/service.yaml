apiVersion: v1
kind: Service
metadata:
  name: code-review-service
  namespace: learnflow
  labels:
    app: code-review-service
spec:
  selector:
    app: code-review-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8002
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: exercise-service
  namespace: learnflow
  labels:
    app: exercise-service
spec:
  selector:
    app: exercise-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8004
  type: ClusterIP

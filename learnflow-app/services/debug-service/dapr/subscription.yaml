apiVersion: dapr.io/v1alpha1
kind: Subscription
metadata:
  name: debug-subscription
  namespace: learnflow
spec:
  pubsubname: pubsub
  topic: learnflow.debugging
  route: /debug
  scopes:
    - debug-service

apiVersion: v1
kind: Service
metadata:
  name: debug-service
  namespace: learnflow
  labels:
    app: debug-service
spec:
  selector:
    app: debug-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8003
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: code-sandbox-service
  namespace: learnflow
  labels:
    app: code-sandbox-service
spec:
  selector:
    app: code-sandbox-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8006
  type: ClusterIP
